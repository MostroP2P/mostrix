# Cross-compilation configuration for mostrix
# https://github.com/cross-rs/cross/wiki/Configuration
#
# No custom image overrides: mostrix uses rustls (sqlx + reqwest), so we do not
# need OpenSSL. The default cross base images are sufficient for all targets
# in archs (Linux musl, Windows gnu, FreeBSD).
